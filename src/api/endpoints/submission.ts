import { API_HOST } from "@/utils/env";


const SUBMISSION_API = {
 SUBMISSION : `${API_HOST}/api/submission`,
 SUBMISSION_CLIENT_FILTER : `${API_HOST}/api/submission/client-filter`,
 
};
export default SUBMISSION_API;